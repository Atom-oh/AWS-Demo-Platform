apiVersion: v1
kind: Service
metadata:
  name: atlantis
  namespace: atlantis
spec:
  type: ClusterIP
  selector: { app: atlantis }
  ports:
    - { name: atlantis, port: 80, targetPort: 4141, protocol: TCP }
