# PR review 패널의 AI CLI 인증 키.
# Secrets Manager `/demo-platform/actions/AI-key` (JSON: KIRO_API_KEY)
#   -> K8s Secret `ai-panel-keys` -> 러너 컨테이너 env (appset).
# Codex 는 Bedrock 네이티브라 키 불필요.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ai-panel-keys
  namespace: actions-runner-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: ai-panel-keys
    creationPolicy: Owner
  data:
    - secretKey: KIRO_API_KEY
      remoteRef: { key: /demo-platform/actions/AI-key, property: KIRO_API_KEY }
