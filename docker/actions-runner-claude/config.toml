# Codex CLI — Bedrock 네이티브. 러너 노드 IAM 자격증명 사용(us-east-1).
model = "openai.gpt-5.6-sol"
model_provider = "amazon-bedrock"
model_reasoning_effort = "xhigh"
approval_policy = "never"

[model_providers.amazon-bedrock.aws]
region = "us-east-1"
